module.exports = async function main ({ rootDirectory }) {
  console.log(
    `
This template is still very much a work in progress.
Note: You must use yarn as we depend on yarn resolutions.
Run "yarn && yarn dev" to get started.
Have fun.
    `.trim()
  )
}
